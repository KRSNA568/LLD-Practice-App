{
  "name": "@lld/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared vocabulary between the API and the web app: the design model, the rubric result shape, and the attempt lifecycle. Zod schemas so the same definition validates at runtime and types at compile time.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
