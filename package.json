{
  "name": "lld-practice-platform",
  "version": "0.1.0",
  "private": true,
  "description": "A focused practice loop for Low-Level Design: choose a problem, submit a structured design, get rubric feedback anchored to evidence in your own submission.",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev --workspace @lld/api",
    "dev:web": "npm run dev --workspace @lld/web",
    "build": "npm run build --workspace @lld/contracts && npm run build --workspace @lld/api && npm run build --workspace @lld/web",
    "seed": "npm run seed --workspace @lld/api",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b apps/api packages/contracts --pretty",
    "gate": "vitest run tests/evaluation/Calibration.test.ts tests/evaluation/Adversarial.test.ts && npm run typecheck",
    "check": "npm run typecheck && vitest run",
    "prepare": "git config core.hooksPath .githooks 2>/dev/null || true"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "concurrently": "^9.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
