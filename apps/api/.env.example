# Local SQLite file. Committed as .env so a fresh clone works with no setup.
DATABASE_URL="file:./dev.db"

# Optional. Without it the three judgement criteria are scored by a deterministic
# heuristic and the whole loop still works.
# ANTHROPIC_API_KEY=sk-ant-...
